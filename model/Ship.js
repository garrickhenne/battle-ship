class Ship {

  // size: number
  // head: first coordinate
  // tail: last coordinate
  constructor(size, head, tail) {
    this._size = size;
    this._head = head;
    this._tail = tail;
  }
}

module.exports = Ship;